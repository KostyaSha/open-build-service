[Unit]
Description=OBS apidelayed daemon
After=syslog.target

[Service]
ExecStart=/usr/sbin/obsapidelayed start
ExecStop=/usr/sbin/obsapidelayed stop
Type=forking
User=wwwrun
Group=www

[Install]
WantedBy=multi-user.target
